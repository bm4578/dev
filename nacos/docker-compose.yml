
version: '3'
services:
  nacos:
    image: nacos/nacos-server:latest
    ports:
      - "8848:8848"
    environment:
      - PREFER_HOST_MODE=hostname
      - NACOS_USER=codetype
      - NACOS_PASS=codetype1234
    volumes:
      - ./data:/home/nacos/data
      - ./logs:/home/nacos/logs
      - ./init.d:/home/nacos/init.d
